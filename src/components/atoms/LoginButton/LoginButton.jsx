import { Button } from "@mui/material";
import React from "react";

export const LoginButton = () => {
  return (
    <Button type="sumbit" variant="contained" sx={{ padding: "7px 50px" }}>
      Login
    </Button>
  );
};
